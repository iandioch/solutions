A graph problem to find the disjoint sets / unconnected components. Union find (here implemented with path compression) works, however the python recursion limit must be increased.

Runs in 0.16s in python3.
